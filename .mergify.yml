queue_rules:
  - name: renovate
    conditions:
      - author=rubiin
      - status-success=build

pull_request_rules:
- name: merge pull requests from renovate bot if CI passes
  conditions:
    - author=rubiin
    - status-success=build
  actions:
    queue:
      name: renovate
